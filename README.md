# ALA 1 - Ejercicio 2: Calculadora básica

## Descripción
Calculadora simple que resuelve las 4 operaciones básicas (suma, resta, multiplicación y división) 
entre una cantidad variable de operandos ingresados por el usuario.
Se presupone que el usuario no va a ingresar datos erroneos.

## Cómo ejecutar
1. Instalar dependencias: `npm install`
2. Ejecutar: `node ejercicio2.js`
3. Ingresar el operador deseado (+, -, *, /), la cantidad de operandos, y cada operando cuando se solicite.

## Estado
- ✅ Versión básica (2 operandos, 1 operador)
- ✅ Bonus (cantidad variable de operandos)